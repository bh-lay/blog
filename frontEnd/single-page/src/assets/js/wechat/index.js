

export default function setData (title, desc, img) {
	// require.ensure(['./wechat.js'], () => {
	// 	// 异步引入分享模块
	// 	let wechat = require('./wechat.js')
	// 	wechat.updatePageInfo(title, desc, img)
	// })
}
